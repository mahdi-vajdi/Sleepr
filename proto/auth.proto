syntax = "proto3";

package auth;

service AuthService {
  rpc Authenticate (Authentication) returns (User) {}
}

message Authentication {
  string Authentication = 1;
}

message User {
  string id = 1;
  string email =2;
  string password = 3;
}